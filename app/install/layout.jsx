export const metadata = {
  title: 'Connect & Install App — AegisFlow',
  description: 'Integrate AegisFlow security checks on GitHub repositories.',
  keywords: 'install, integration, webhook configuration, github app, AegisFlow',
};

export default function InstallLayout({ children }) {
  return children;
}
