export const metadata = {
  title: 'Secure Access & Sign In — AegisFlow',
  description: 'Log in or sign up to configure automatic code vulnerability reviews on your pipeline.',
  keywords: 'sign in, login, oauth login, signup, AegisFlow auth',
};

export default function LoginLayout({ children }) {
  return children;
}
