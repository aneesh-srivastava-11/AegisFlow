export const metadata = {
  title: 'Repository Security Console — AegisFlow',
  description: 'Monitor pipeline health, recent analysis runs, and active issues.',
  keywords: 'dashboard, metrics, security logs, code review reports',
};

export default function DashboardLayout({ children }) {
  return children;
}
