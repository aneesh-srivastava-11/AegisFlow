export const metadata = {
  title: 'Global Settings Console — AegisFlow',
  description: 'Control AI review configurations, prompt criteria parameters, and monitor webhook logging activity.',
  keywords: 'admin console, configuration settings, logging monitor',
};

export default function AdminLayout({ children }) {
  return children;
}
